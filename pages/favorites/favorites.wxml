<view class="container">
  <!-- Tabs -->
  <view class="tabs">
    <view class="tab-item {{activeTab === 0 ? 'active' : ''}}" bindtap="onTabClick" data-index="0">收藏导师</view>
    <view class="tab-item {{activeTab === 1 ? 'active' : ''}}" bindtap="onTabClick" data-index="1">收藏项目</view>
    <view class="tab-item {{activeTab === 2 ? 'active' : ''}}" bindtap="onTabClick" data-index="2">关注风险</view>
  </view>

  <!-- Content -->
  <scroll-view class="content-scroll" scroll-y="true">
    <!-- Favorite Tutors -->
    <view class="list-container" wx:if="{{activeTab === 0}}">
      <view class="tutor-card" wx:for="{{tutorList}}" wx:key="id" bindtap="navigateToDetail" data-id="{{item.id}}">
        <view class="card-header">
          <image class="avatar" src="{{item.avatar}}"></image>
          <view class="header-info">
            <view class="name-row">
              <text class="name">{{item.name}}</text>
            </view>
            <view class="school">{{item.school}} · {{item.department}}</view>
            <view class="direction">{{item.direction}}</view>
            <view class="date">收藏于 {{item.date}}</view>
          </view>
        </view>
      </view>
    </view>

    <!-- Favorite Projects -->
    <view class="list-container" wx:if="{{activeTab === 1}}">
      <view class="project-card" wx:for="{{projectList}}" wx:key="id">
        <view class="project-title">{{item.title}}</view>
        <view class="project-leader">负责人：{{item.leader}}</view>
        <view class="project-progress">
          <text>项目进度</text>
          <progress percent="{{item.progress}}" stroke-width="6" activeColor="#2b7fff" />
          <text>{{item.progress}}%</text>
        </view>
        <view class="date">收藏于 {{item.date}}</view>
      </view>
    </view>

    <!-- Risk -->
    <view class="list-container" wx:if="{{activeTab === 2}}">
      <view class="risk-card" wx:for="{{riskList}}" wx:key="id">
        <view class="risk-header">
          <view class="risk-title">{{item.tutor}}</view>
          <view class="risk-tag">低风险</view>
        </view>
        <view class="risk-type">风险类型：{{item.type}}</view>
        <view class="date">最后更新 {{item.date}}</view>
      </view>
    </view>
  </scroll-view>
</view>
